{
  "name": "persons_web",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js ",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push",
    "clean": "rmdir /s dist",
    "build:ui": "rmdir /s /q dist  && cd ./phonebook && npm run build && xcopy dist ..\\..\\persons_web\\dist /E /I /Y",
    "mongo": "node mongo testi1234",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "JP",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.4.4",
    "morgan": "^1.10.0",
    "nodemon": "^3.1.4"
  }
}
